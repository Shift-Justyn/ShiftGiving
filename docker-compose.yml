version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: shiftgiving-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: shiftgiving
      POSTGRES_USER: shiftgiving
      POSTGRES_PASSWORD: shiftgiving_local_dev
      POSTGRES_INITDB_ARGS: "--encoding=UTF8"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./api/ShiftGiving/Data/seed_data.sql:/docker-entrypoint-initdb.d/seed_data.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U shiftgiving -d shiftgiving"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - shiftgiving-network

volumes:
  postgres_data:
    driver: local

networks:
  shiftgiving-network:
    driver: bridge
